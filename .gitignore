# 콘티 패널 원본 — 용량이 커서 저장소에 넣지 않는다. --panels로 재생성한다.
panels/

# 작업 파일
panel-instruction.txt

# 영상 (입력·번인 결과)
*.mp4

# OS
Thumbs.db
.DS_Store

# examples/ 의 contisheet.png 는 쇼케이스용이라 예외로 추적한다
!examples/*/contisheet.png
